#include<stdio.h>
int main()
{
int age;
printf("enter your age:\n");
scanf("%d",&age);
(age>=21)?(printf("age is suitable for marrying")):(printf("age is not suitable for marrying"));
return 0;
}